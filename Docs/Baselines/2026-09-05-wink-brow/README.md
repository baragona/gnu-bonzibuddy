# Winking brow

The left brow lowers with the winking eyelid and relaxes as it reopens, using 40% of the existing imported brow-down morph. Independent brow intentions share facial transition blending. A smooth center mask separates the fan morph into anatomical sides; the same code deforms the visible mesh, shadows, and audit geometry. Explicit manual brow-down overrides take priority.

Actual GPU mesh comparison at the identical held head pose found 542 changed vertices on the intended side, maximum vertical lowering 0.016545 character units, and zero displacement on the opposite side outside the blended center strip. Manual-zero override passed. Reviewed the held and reopened pose from multiple angles; the three-angle motion preview is included.

All 900 skeletal transition pairs, 2,880 facial cases, and 20 routine contracts passed. The head-motion check still reports a stationary hold and one acceleration/deceleration per turn. Neutral appearance remains pixel-identical to the rest-contact checkpoint. Eye counter-motion and the corrected head timing are unchanged. This is a wink refinement, not completion of the original repertoire or a live frame-rate claim.
